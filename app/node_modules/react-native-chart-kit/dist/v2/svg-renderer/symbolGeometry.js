export const createSvgSymbolDiamondPath = ({ size, x, y }) => {
    const radius = size / 2;
    return [
        `M ${x} ${y - radius}`,
        `L ${x + radius} ${y}`,
        `L ${x} ${y + radius}`,
        `L ${x - radius} ${y}`,
        "Z"
    ].join(" ");
};
