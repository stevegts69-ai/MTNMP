const sanitizePart = (part) => {
    return part
        .trim()
        .toLowerCase()
        .replace(/[^a-z0-9_-]+/g, "-")
        .replace(/^-+|-+$/g, "");
};
export const createSvgTestId = (...parts) => {
    return parts.map(String).map(sanitizePart).filter(Boolean).join(".");
};
