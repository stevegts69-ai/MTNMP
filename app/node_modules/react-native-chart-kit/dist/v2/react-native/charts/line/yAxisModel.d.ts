import { type CartesianSeriesInput, type NormalizedCartesianData, type NumericDomainInput } from "../../../core/index";
import { type LineChartYAxisLabelWidth } from "./axisLabels";
type AxisLabelSize = {
    width: number;
    height: number;
};
type TextMeasurer = (text: string, options?: Record<string, unknown>) => AxisLabelSize;
type ResolveLineChartYAxisModelOptions<TData extends Record<string, unknown>> = {
    data: TData[];
    stableData?: TData[] | undefined;
    xKey: keyof TData & string;
    series: Array<CartesianSeriesInput<TData>>;
    yDomain: NumericDomainInput;
    yAxisLabelWidth?: LineChartYAxisLabelWidth | undefined;
    formatYLabel: (value: number) => string;
    measureText: TextMeasurer;
    textOptions: Record<string, unknown>;
};
export declare const resolveLineChartYAxisModel: <TData extends Record<string, unknown>>({ data, stableData, xKey, series, yDomain, yAxisLabelWidth, formatYLabel, measureText, textOptions }: ResolveLineChartYAxisModelOptions<TData>) => {
    normalized: NormalizedCartesianData<TData>;
    yDomainResolved: [number, number];
    yLabelSizes: {
        width: number;
        height: number;
    }[];
    yTicks: number[];
};
export {};
//# sourceMappingURL=yAxisModel.d.ts.map